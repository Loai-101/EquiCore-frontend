/**
 * Redux store setup — wire up when @reduxjs/toolkit is added.
 * Example:
 *   import { configureStore } from '@reduxjs/toolkit';
 *   export const store = configureStore({ reducer: { ... } });
 */

export const store = null;
