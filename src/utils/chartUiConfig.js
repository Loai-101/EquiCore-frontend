/**
 * Recharts presentation tokens — kept out of JSX so the dashboard component stays readable.
 * (Library still applies these via props; swap for theme provider when design system grows.)
 */
export const stableDashboardChartAxisTick = {
  fill: '#5c657a',
  fontSize: 12,
};

export const stableDashboardChartTooltipContentStyle = {
  borderRadius: 12,
  border: '1px solid rgba(21,36,71,0.08)',
};
